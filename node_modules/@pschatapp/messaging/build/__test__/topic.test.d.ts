import { Replies } from "amqplib";
import { Publisher } from "../base";
import { UserVerifiedEvent } from "../events";
import { RoutingKey } from "../routing";
export declare class UserVerifiedEventPublisher extends Publisher<UserVerifiedEvent> {
    onSuccess(err: any, ok: Replies.Empty): void;
    onFail(err: any, ok: Replies.Empty): void;
    routingKey: RoutingKey;
    expiration: number;
    mandatory: boolean;
    persistent: boolean;
    constructor();
}
