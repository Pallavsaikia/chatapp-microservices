"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RoutingKeyGenerator = void 0;
class RoutingKeyGenerator {
    constructor(...args) {
        this.keyArray = args;
    }
    toString() {
        const keyGen = [];
        for (let i = 0; i < this.keyArray.length; i++) {
            if (this.keyArray[i] === null) {
                if (this.keyArray[i + 1] === null) {
                    while (this.keyArray[i + 1] === null) {
                        i++;
                    }
                    keyGen.push("#");
                }
                else {
                    keyGen.push("*");
                }
            }
            else {
                keyGen.push(this.keyArray[i]);
            }
        }
        return keyGen.reduce((previousvalue, currentvalue) => previousvalue + "." + currentvalue);
    }
}
exports.RoutingKeyGenerator = RoutingKeyGenerator;
